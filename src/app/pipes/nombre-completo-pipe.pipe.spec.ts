import { NombreCompletoPipePipe } from './nombre-completo-pipe.pipe';

describe('NombreCompletoPipePipe', () => {
  it('create an instance', () => {
    const pipe = new NombreCompletoPipePipe();
    expect(pipe).toBeTruthy();
  });
});
